# TODO: Restructure Landing Page Layout

## Steps to Complete
- [x] Update the outer split section Box: Remove height: "100vh" and set to auto for normal flow.
- [x] Modify LEFT HALF (Visible Light): Create a styled card Box with background image, height 400px, border, and shadow. Move Typography content below the card in normal flow.
- [x] Modify RIGHT HALF (NIR): Create a styled card Box with background image, height 400px, border, and shadow. Move Typography content below the card in normal flow.
- [x] Ensure responsive design: On xs, cards stack vertically with content below each.
- [ ] Test the layout: Run the app and verify content is visible below images without overlays.
